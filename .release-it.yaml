# DO NOT EDIT THIS FILE AS IT IS GENERATED BY @r2d2bzh/js-backend-rules
npm:
  publish: false
hooks:
  before:bump:
    - sed -i -e 's/${latestVersion}/${version}/g' helm/Chart.yaml
    - (cd apidoc && release-it --ci ${version})
    - (cd dev/gateway && release-it --ci ${version})
    - (cd open-api && release-it --ci ${version})
    - (cd openapi && release-it --ci ${version})
    - (cd share && release-it --ci ${version})
    - (cd test && release-it --ci ${version})
    - (cd test-moleculer-web && release-it --ci ${version})
