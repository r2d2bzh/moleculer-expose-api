# THIS FILE IS PARTIALLY GENERATED BY @r2d2bzh/js-backend-rules
# The following fields are generated: apiVersion, name, version, description, appVersion
# If these fields need to be overridden, add them in the following section to
# the package.json file of the root project:
# { "r2d2bzh": { "helm": { "chart": { ... } } } }
apiVersion: v1
name: '@r2d2bzh/moleculer-to-openapi'
version: 0.0.0
description: ''
appVersion: 0.0.0
